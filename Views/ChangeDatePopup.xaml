<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Aeonpulse.Views.ChangeDatePopup"
             BackgroundColor="#80000000"
             Shell.NavBarIsVisible="False">

    <ContentPage.Content>
        <Grid VerticalOptions="Center"
              HorizontalOptions="Center"
              Margin="24,0">

            <Frame BackgroundColor="{DynamicResource CardBackground}"
                   CornerRadius="16"
                   Padding="24"
                   HasShadow="True"
                   BorderColor="{DynamicResource CyberPurple}">

                <VerticalStackLayout Spacing="16">

                    <!-- Title -->
                    <Label Text="Change Date"
                           FontSize="{StaticResource FontSizeTitle}"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="{DynamicResource CyberCyan}" />

                    <!-- Descriptive text -->
                    <Label Text="Track metrics from any significant life event - your birthday, graduation, wedding, or any milestone that matters to you."
                           FontSize="{StaticResource FontSizeMedium}"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           TextColor="{DynamicResource TextGray}" />

                    <!-- Divider -->
                    <BoxView HeightRequest="1"
                             Color="{DynamicResource CyberPurple}"
                             Opacity="0.4"
                             HorizontalOptions="Fill" />

                    <!-- Event Name -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Event Name"
                               FontSize="{StaticResource FontSizeMedium}"
                               FontAttributes="Bold"
                               TextColor="{DynamicResource CyberPurple}" />
                        <Frame Padding="10,8"
                               CornerRadius="8"
                               BorderColor="{DynamicResource CyberPurple}"
                               BackgroundColor="{DynamicResource SpaceDark}"
                               HasShadow="False">
                            <Entry x:Name="EventNameEntry"
                                   Placeholder="e.g. My Birthday"
                                   PlaceholderColor="{DynamicResource TextGray}"
                                   TextColor="{DynamicResource TextWhite}"
                                   BackgroundColor="Transparent"
                                   FontSize="{StaticResource FontSizeLarge}"
                                   ReturnType="Next" />
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Date -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Date"
                               FontSize="{StaticResource FontSizeMedium}"
                               FontAttributes="Bold"
                               TextColor="{DynamicResource CyberPurple}" />
                        <Frame Padding="10,4"
                               CornerRadius="8"
                               BorderColor="{DynamicResource CyberPurple}"
                               BackgroundColor="{DynamicResource SpaceDark}"
                               HasShadow="False">
                            <DatePicker x:Name="EventDatePicker"
                                        TextColor="{DynamicResource TextWhite}"
                                        BackgroundColor="Transparent"
                                        FontSize="{StaticResource FontSizeLarge}"
                                        Format="yyyy-MM-dd"
                                        HorizontalOptions="Fill" />
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Divider -->
                    <BoxView HeightRequest="1"
                             Color="{DynamicResource CyberPurple}"
                             Opacity="0.4"
                             HorizontalOptions="Fill" />

                    <!-- Buttons -->
                    <Grid ColumnDefinitions="*,12,*"
                          HorizontalOptions="Fill">

                        <Button Grid.Column="0"
                                x:Name="CloseButton"
                                Text="Close"
                                Clicked="OnCloseClicked"
                                BackgroundColor="{DynamicResource SpaceDark}"
                                TextColor="{DynamicResource TextGray}"
                                CornerRadius="8"
                                FontSize="{StaticResource FontSizeLarge}"
                                HeightRequest="44"
                                BorderColor="{DynamicResource CyberPurple}"
                                BorderWidth="1" />

                        <Button Grid.Column="2"
                                x:Name="OkButton"
                                Text="OK"
                                Clicked="OnOkClicked"
                                BackgroundColor="{DynamicResource CyberPurple}"
                                TextColor="{DynamicResource SpaceDarker}"
                                CornerRadius="8"
                                FontSize="{StaticResource FontSizeLarge}"
                                FontAttributes="Bold"
                                HeightRequest="44" />

                    </Grid>

                </VerticalStackLayout>
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage>